services:
  - type: web
    name: aquelia-backend
    env: node
    plan: free
    buildCommand: npm ci
    startCommand: bun run server.ts
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000 